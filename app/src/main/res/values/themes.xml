<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Base application theme. -->
  <style name="Base.AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Customize your theme here. -->
    <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
  </style>

  <style name="AppTheme" parent="Base.AppTheme" />

  <!-- Tema para el botón de cast: ícono siempre blanco (visible sobre fondo oscuro) -->
  <style name="Theme.MediaRouter.White" parent="Theme.MediaRouter">
    <item name="mediaRouteDefaultIconDrawable">@drawable/ic_cast_white</item>
    <item name="mediaRouteButtonTint">#FFFFFF</item>
  </style>
</resources>